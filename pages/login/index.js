//import libraries
import { Form, Formik } from "formik";
import Link from "next/link";
import React, { useState, useEffect, Fragment } from "react";
import { Box, Input, LoginForm } from "../../components/allComponents";

// COMPONENT
const Login = ({ props }) => {
  // Logic

  return (
    <Fragment>
      <LoginForm />
    </Fragment>
  );
};

export default Login;
