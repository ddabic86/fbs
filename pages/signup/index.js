//import libraries
import React, { useState, useEffect, Fragment } from "react";
import { Box, RegisterForm } from "../../components/allComponents";

// COMPONENT
const Index = ({ props }) => {
  // Logic

  return (
    <Box>
      <RegisterForm />
    </Box>
  );
};

export default Index;
